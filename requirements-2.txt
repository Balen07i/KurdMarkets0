# --- Telegram Bot Framework ---
# Range (not exact pin): aiogram caps its own pydantic upper bound and
# that cap has moved across releases (3.15.0 -> pydantic<~2.10;
# 3.30.0 -> pydantic<2.14). Pinning both aiogram and pydantic to exact
# versions from different points in that history is what caused
# ResolutionImpossible. Let pip pick a mutually-compatible pair.
aiogram>=3.20.0,<4.0.0

# --- Database ---
SQLAlchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10

# --- Caching / Queue backing ---
redis==5.2.1

# --- Scheduling ---
APScheduler==3.10.4

# --- HTTP / Scraping ---
# httpx 0.28 dropped the `proxies` Client kwarg; older anthropic releases
# (incl. 0.40.0 below) pinned httpx<0.28 as a result. Range avoids forcing
# a version outside whatever anthropic's installed release requires.
httpx>=0.27.0,<1.0.0
beautifulsoup4==4.12.3
lxml==5.3.0
# 0.3.x has no Python 3.13 wheels (cp313 wheels start at 0.4.x) — bumped.
selectolax==0.4.11

# --- Configuration & Validation ---
# Range: must stay under aiogram's pydantic upper bound (see note above).
pydantic>=2.9.0,<2.14.0
pydantic-settings>=2.7.0,<3.0.0
python-dotenv==1.0.1

# --- Logging ---
structlog==24.4.0

# --- AI (Kurdish summary generation) ---
# 0.40.0 pinned httpx<0.28, conflicting with the httpx range above.
# Modern anthropic releases require httpx>=0.25.0,<1 (no upper cap).
anthropic>=0.60.0,<1.0.0

# --- Utilities ---
tenacity==9.0.0
pendulum>=3.0.0,<4.0.0
orjson==3.10.12

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
freezegun==1.5.1

# --- Dev tooling ---
ruff==0.8.4
mypy==1.13.0
